---
id: counsel
title: 상담 — 재원생 / 신입생 두 갈래
type: system
status: verified
source: api/index.js:315-741, api/admissionModule.js, api/confirmNotifyModule.js, docs/입학상담-appsscript.md
updated: 2026-08-15
tags: [counsel, admission, alimtalk, googleform]
---

## 한 줄

이름이 비슷해서 헷갈리지만 **완전히 다른 두 시스템**이다. DB도 흐름도 다르다.

| | 재원생 상담 | 신입생(입학) 상담 |
|---|---|---|
| 입구 | 자체 폼 `/counsel` | 구글폼 |
| 처리 | 우리 서버 `POST /api/counsel` | 응답 시트의 **Apps Script** |
| 노션 DB | `COUNSEL_DB_ID` `3b109320…` | `ADMISSION_DB_ID` `1a109320…` |
| 카카오워크 | 재원생 상담알림_BOT | 신입생 상담알림_BOT |
| 학부모 발송 | 접수 확인 + 통화 확정 | 예약 확인 |

> 🔴 **`상담신청서 관리`(`18609320…`)는 2025-07-21에 멈춘 옛 폼이다.** 이름이 비슷해 거기다 작업했다가 되돌린 적 있다. 신입생 상담은 반드시 `1a109320…`.

## 재원생 상담 — 흐름

구글폼을 안 거치고 서버가 직접 받는다. 학원 디자인 그대로 열리고 고장 날 지점이 하나 줄어든다. 대신 **구글폼이 공짜로 주던 안전망(응답이 시트에 남는 것)이 없으므로, 노션 기록이 실패하면 신청 원문을 통째로 카카오워크와 원장 문자에 실어 보낸다.** 신청이 조용히 사라지는 게 최악이다.

이름으로 학생 명부를 찾아 담임·학생ID·연락처를 자동으로 채운다(`lookupStudentByName`, `api/index.js:429` — `OK`/`UNMATCHED`/`DUPLICATE`).

## 계약 (건드리면 안 되는 것)

- 🔴 **공용 채널과 담임 DM에 같은 내용이 가야 한다.** 예전엔 채널에 학생 이름만 가고 **문의 원문이 빠져서**, 원장이 "내가 챙길지 담임에게 맡길지"를 채널만 보고 판단할 수 없었다. 상담은 원장이 직접 받아야 하는 건이 섞여 있다. **요약해서 보내지 말 것.**
- **담임 DM을 먼저 보내고 그 성패를 채널 메시지에 싣는다.** 담임에게 못 갔으면 채널이 유일한 통로라 눈에 띄어야 한다.
- 🔴 **접수 확인 알림톡은 보낸다.** `ALIMTALK_TPL_COUNSEL_RECEIPT`, 변수 `#{이름}`·`#{날짜}`. **상담은 결석과 다르다** — 결석엔 보강 확정 안내가 뒤따르지만 상담은 카톡 답장으로 끝나는 건이 대부분이라 확정 안내가 영영 안 나간다. **이 한 통이 학부모가 받는 유일한 회신인 경우가 많다.** 본문의 "수업이 끝난 뒤 순차적으로 연락드린다"가 핵심 — 바로 전화가 안 와도 기다릴 수 있게 한다.
- **`상담예약`·`상담예약 안내확인` 템플릿은 쓸 수 없다** — 신입생 방문 예약용이라 레테비·주차 안내가 들어 있다.
- **통화 확정 알림톡**은 `api/confirmNotifyModule.js`에 있다 — **보강 확정과 같은 모듈·같은 구조. 새로 만들지 말고 거기 붙일 것.** `ALIMTALK_TPL_COUNSEL_CONFIRM`, 변수 3개. 발송 판정은 체크박스가 아니라 `확정발송일시`.
- **안내 문구는 전화가 아니라 카톡 답장 기준이다.** 예전 "전화드립니다"가 실제 흐름과 달라 고쳤다(폼 상단·도움말·완료 화면·접수 문자 네 군데에 흩어져 있었다).

## 방치 알림 + 완료 버튼

**매일 16:00**, `상태=접수`로 하루 넘은 건을 담임별로 묶어 DM(`remindStaleCounsel`, `api/index.js:715`). 5분마다 조르면 도배라 하루 한 번. 담임 미연결·미지정 건은 원장에게 모아 보낸다.

🔴 그 DM에 **학생마다 `✅ ○○ 완료` 버튼**이 달려 있다. **노션을 열게 하면 아무도 안 한다.** `GET /api/counsel/done?id=&t=` — HMAC 서명(`{id}:done`), 두 번 눌러도 안전, **버튼은 5개까지만**(그 이상은 카드가 길어져 안 읽는다). 교재비 승인 버튼과 같은 방식.

## 신입생 상담

구글폼 → 시트 → **Apps Script**가 노션 기록 + 카카오워크 알림. **우리 서버를 안 거친다**(서버가 죽어도 신청이 유실되지 않는다). 코드는 `docs/입학상담-appsscript.md`. 노션을 **먼저 쓰고** 만들어진 페이지 주소를 카카오워크 버튼으로 단다. 스크립트 속성 2개 필요(`KAKAOWORK_APP_KEY`, `NOTION_TOKEN`), `checkSetup()`으로 점검.

**예약 확인 알림톡은 서버가 보낸다** (`api/admissionModule.js`, 5분 크론): 원장이 `상담 예약일`·`💌 상담 코멘트`를 쓰고 **`상담예약함`**을 체크 → 발송 → `알림톡 발송완료` 체크.

- 템플릿 `KA01TP250223163830368xwWO2Ze1CcQ`. `#{상담예약일}`은 **자유 텍스트** — 적은 그대로 나가므로 날짜 포맷 불필요
- 전화번호는 **`전화번호`(텍스트) 우선.** `전화번호 1`(phone_number)은 손으로 넣는 자리라 비어 있다
- 중복은 `알림톡 발송완료`로 막는다. 발송 직전에 켜고 실패하면 되돌린다. 다시 보내려면 그걸 끄고 `상담예약함`을 켠다

관련: [[absence-notice]] · [[kakao-channel-bot]] · [[alimtalk-send]] · [[kakaowork-notify]] · [[cron-jobs]]
