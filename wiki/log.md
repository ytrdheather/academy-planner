# 작업 이력

append-only. 새 항목은 맨 아래에. 포맷은 `wiki/SCHEMA.md` 참조.

```bash
grep "^## \[" wiki/log.md | tail -10
```

---

## [2026-08-15] bootstrap | 위키 개설

Karpathy LLM-wiki 패턴으로 `wiki/` 층 신설. 목적은 세션마다 반복되는 코드 재탐색 비용 제거.

- 뼈대: `CLAUDE.md`(루트, 짧게), `wiki/SCHEMA.md`, `wiki/index.md`, `wiki/log.md`
- 언어 정책: 파일명·태그·식별자 영어, 본문 한국어 (하이브리드)
- 위치: repo `/wiki`, git 추적. 기존 `docs/` 5개 문서는 손대지 않고 raw source로 인용만
- `status: verified|inferred|stale` 로 페이지 신뢰도를 표시하기로 함

## [2026-08-15] ingest | 1차 33페이지 작성

`api/` 코드와 `docs/`, 메모리 17개를 원본으로 삼아 33페이지 작성.

| 디렉토리 | 개수 | 무엇 |
|---|---|---|
| `patterns/` | 9 | 공용 헬퍼 — `api/index.js` 상단을 직접 읽고 씀 |
| `entities/` | 8 | Notion DB 맵, 환경변수, 크론 13개, 라우트, 화면 28개, 외부 서비스, Solapi·KakaoWork 실측 |
| `systems/` | 9 | 교재비·결석·상담·숙제·데일리·문법·시험·월간·챗봇 동작 계약 |
| `decisions/` | 4 | Render 수동배포, Supabase 보류, Make 이전, 브랜드 |
| `pitfalls/` | 3 | 담임 롤업 사고, 발신번호 오타, 노션 지연 |

슬래시 커맨드 3종 신설: `/wiki-ingest` `/wiki-query` `/wiki-lint` (`.claude/commands/`).
`MEMORY.md` 머리에 "상세는 위키" 포인터 추가 + 항목마다 대응 위키 페이지 명시. **메모리 본문은 지우지 않았다** — 위키가 몇 번 검증될 때까지 원본으로 남겨 둔다.

## [2026-08-15] lint | 첫 검진 — 링크·index·source 전부 통과

| 항목 | 결과 |
|---|---|
| 페이지 수 | 33 |
| 끊긴 링크 | `progress-automation-design` 1건 — **index의 "미작성" 표에 등재됨(정상)** |
| 고아 페이지 | 없음 |
| index 누락 | 없음 |
| `source:` 라인 검증 | **30건 전수 확인, 전부 일치** (`api/index.js` 19건 + 모듈 11건) |
| `status: inferred` | 1건 — `systems/monthly-report` (`monthlyReportModule.js` 본문 미정독) |
| 모순 | 1건 발견·해소 → 아래 |
| 40줄 초과 | 6건 (표 위주라 허용, 다음 lint에서 재검토) |

**해소한 모순**: `memory/readiplan-brand.md`(2026-07-21)의 "푸시 시 Render auto-deploy 트리거"는 틀렸다. `memory/render-manual-deploy.md`(2026-08-07)가 최신이고 **자동 배포는 꺼져 있다.** 두 위키 페이지 양쪽에 경고를 박아 뒀다.

**정정**: 작업 중 크론 타임존을 잘못 적었다가 고쳤다 — **13개 크론 전부 `{ timezone: 'Asia/Seoul' }`을 갖고 있다.** 다만 크론 콜백 *안*의 맨 `new Date()`는 여전히 서버 시간이며, `monthlyReportModule.js:593`이 그 패턴을 쓴다.

### 다음에 팔 것

1. `api/monthlyReportModule.js` 정독 → `systems/monthly-report`를 `verified`로
2. `.env.example`이 심하게 낡음(Vercel·Replit 시절, 실사용 변수 대부분 누락) → `entities/env-vars` 기준으로 재작성
3. `patterns/` 미작성 후보 — 노션 페이지네이션(100건 상한 처리), `teacherDm` 카드 메시지 구조
4. `progress-automation-design` 페이지 (`docs/진도자동화-핸드오프.md` 기반)

## [2026-08-15] ingest | 스킬 층 신설 — CLAUDE.md 를 안 키우기 위해

`CLAUDE.md` 는 **매 세션 자동 로드**라 여기 붙는 한 줄은 모든 세션이 값을 치른다. 그래서 절차를 이 파일이 아니라 스킬로 뺀다. **상한 200줄, 목표 50줄 이하**를 `CLAUDE.md` 안에 규칙으로 못 박았다 (현재 48줄).

### 세 층의 역할 분담 (이번에 확정)

| 어디에 | 무엇을 | 비용 |
|---|---|---|
| `CLAUDE.md` | 몰랐을 때 사고가 나는 것 + 한 줄 요약·포인터만 | 매 세션 |
| `.claude/commands/` | 여러 위키 페이지에 걸친 다단계 절차 | 그 작업 할 때만 |
| `wiki/` | 사실·계약·근거·이력. **기본값** | 열었을 때만 |

### 새 스킬 5종

| 스킬 | 왜 스킬인가 |
|---|---|
| `add-parent-message` | 되돌릴 수 없는 발송. 이미 반려된 알림 목록 확인 → 템플릿 심사 → env → 변수명 대조 → 1건 실발송 → `/messages` 되읽기. 위키 4페이지에 걸친다 |
| `add-cron` | timezone 누락 · 콜백 안 `new Date()` · 수동 트리거 라우트 · **배포 회피 시간대 등록**까지 빠뜨리기 쉽다 |
| `add-module` | `index.js`(3,782줄)를 더 키우지 않게. 주입 목록·`try/catch`·위키 갱신 대상이 흩어져 있다 |
| `deploy-check` | 자동 배포 꺼짐 + 배포가 크론을 죽인다. 시각 판단이 필요하다 |
| `diagnose-not-sent` | 사고가 두 번 났던 영역. 채널별 원인이 다르고 **시간순 정렬**을 안 하면 오진한다 |

**원칙: 스킬은 위키를 복제하지 않고 조율한다.** 내용은 위키에 두고 스킬은 순서와 포인터만 갖는다 — 두 곳에 같은 사실을 쓰면 갈라진다.

### 점검

- `CLAUDE.md` 48줄 / 3,051자 (상한 200줄의 24%)
- 스킬 8개 평균 47줄
- 스킬이 가리키는 위키 경로 **19건 전수 확인, 전부 존재**
- frontmatter YAML 파싱 8건 전부 정상 — `diagnose-not-sent`의 `description`이 따옴표로 시작해 깨졌던 것을 고쳤다

## [2026-08-17] ingest | 학사일정 기간 일정 + 달력 모듈 분리

**요청**: 학부모 공지에서 전·후 달 학사일정을 보고, 다음 달을 미리 만들어 저장하고, "18일부터 20일까지 학원방학"처럼 기간 이벤트를 지정하고 싶다.

**먼저 확인한 것 — 절반은 이미 있었다.** `/notice`의 ◀▶ 월 이동, `/calendar`의 ◀▶ 로 미래 달 미리 작성, 과거 기록 보존(노션), 학부모 화면의 `~` 기간 표시 코드(`fmtRange`)까지 전부 있었다. 실제로 막혀 있던 것은 두 개뿐이었다.

| 막혀 있던 것 | 왜 문제였나 |
|---|---|
| 기간 입력 수단이 없음 | POST가 `start`만 썼다. 18~20일 방학이 점 3개·줄 3개로 나갔다 |
| 이벤트 이름 입력칸이 없음 | 제목이 `"이벤트"`로 저장되고, 학부모 목록은 `내용` 있는 것만 띄워서 **보라색 점만 보였다**. 무슨 행사인지 알 수 없었다 |

**한 것**

- `api/calendarModule.js` 신설 ([[module-di]] 규칙, `try/catch` 포함). `index.js`에서 147줄 제거.
- 기간 일정: 달력에서 시작일·종료일을 차례로 누르면 묶인다. 주 단위로 끊어 **알약 모양**으로 그리고 이름을 그 아래 적는다 → PNG로 내보내도 읽힌다.
- 🔴 **기간의 주인은 시작일이 있는 달.** 달 넘는 기간(12/28~1/3)이 반대쪽 달을 저장할 때 사라지지 않게 하는 핵심 규칙. 넘어온 달에서는 읽기 전용으로 보여주고(안 보여주면 두 번 찍는다) **시수 계산에는 넣는다.**
- 중복 판정 키 `날짜|유형` → `시작일|종료일|유형|제목`. 같은 날 다른 이름의 행사 두 개가 가능해졌다.
- 학부모 화면: 걸치는 날 전부 칠하고, 이름만 지어도 목록에 뜨고, 목록을 "걸치는 달" 기준으로 모은다. 범례에 **학원 행사**(보라) 추가 — 양쪽 달력 모두 빠져 있었다.
- 보강일은 손대지 않았다. 결석 폼 선택지라 하루 단위·시간 메모 유지.

**검증**: 9~10월에 실제 저장/조회/삭제까지 돌리고 원복. 8월 기존 4건 무손상. 달 넘는 기간을 10월에서 빈 상태로 저장해도 9월 것이 남는 것 확인. 넘어온 휴강이 10월 시수에서 빠지는 것 수치로 확인(월수금 13→12, 화목 9→8).

**갱신한 페이지**: [[absence-notice]] (기간 규칙·학부모 월 이동 절 추가, source 갱신)

## [2026-08-18] ingest | 휴강 → 정지 기간 자동 반영 (사고 대응)

**사고**: 2026-08-17 광복절 대체공휴일. 달력에는 휴강이 찍혀 있었는데 **진도 행 57건이 생성되고 리포트 URL까지 채워졌다.** 원인은 두 겹이었다.

| 층 | 상태 |
|---|---|
| 달력의 휴강 | 표시일 뿐, 크론이 보지 않는다 |
| 숙제 생성(11:00) | 정지 기간 DB만 본다. 8/17이 등록돼 있지 않았다 |
| **리포트 생성(10:20)** | **정지 기간을 아예 보지 않았다** |

발송은 화수목금토만 도는 Make라 8/17(월)은 나가지 않았다. 운이 좋았다.

**한 것**

- `generateDailyReports()`가 정지 기간을 보게 했다. 수동 라우트는 `force:true`로 무시 가능.
- 달력 저장 시 **휴강만** 정지 기간 DB에 맞춘다. 🔴 사람이 만든 행은 안 건드리고, 우리 것만 사유에 `[달력]` 태그를 붙여 그것만 지우고 만든다. 같은 기간이 이미 활성이면 또 만들지 않는다.
- 🔴 정지 기간 반영이 실패해도 달력 저장은 성공으로 돌려준다 — 500을 내면 원장이 재저장을 눌러 앞의 쓰기가 두 번 돈다.

**수습**: 8/17 행 57건은 **지우지 않았다.** 열어 보니 비어 있지 않고 **수요일(8/19) 숙제가 배정돼 있었고 진도 커서도 이미 앞으로 가 있었다** — 지우면 숙제가 사라지고 커서만 앞선 채 남는다. 대신 `데일리리포트URL` 57건만 비워 발송 대상에서 뺐다.

**교훈**: "빈 행"인지 판단할 때 `작성완료`·코멘트만 보면 안 된다. 숙제 배정은 다른 칸에 들어 있다.

**갱신**: [[absence-notice]] · 매뉴얼 `📅 공지사항·학사일정`

## [2026-08-20] ingest | 남은 횟수 배지 계산 통일 + 교재 이름 NBSP 사고

- 새 페이지 [[homework-track-move]] — `<< >>` 와 배지. 배지 공식이 화면·서버 두 곳에 있어 값이 어긋나던 것을 통일(길이=목차 마지막 순번, 나눗수=주간 평균). 실데이터 4000건 대조 불일치 0.
- 새 페이지 [[textbook-name-whitespace]] — 교재 제목 끝 NBSP로 배정이 막힘. 양쪽 정규화 + 노션 제목 11권 정리(교재 551권 검증, 실패 0).
- [[homework-automation]] 의 옛 `↩`/`→` 버튼 설명을 `<< >>` 로 교체(라우트는 2026-08-14 에 이미 제거돼 있었다).
- [[notion-databases]] 주의에 title 키 금지 + `총유닛수`는 목차 파생값 두 줄 추가.

## [2026-08-22] ingest | 교재비 일회성 발송(`TEXTBOOK_ONESHOT_AT`)

- 금 21:00 배치를 놓치는 일이 **두 번째**였다(08-07 늦은 승인, 08-21 전부 `승인대기`). 그때마다 손으로 `/send-batch` 를 돌리는 대신 **환경변수로 시각을 걸 수 있게** 했다. 5분 크론에 얹어서 크론 개수는 그대로.
- [[cron-jobs]] 표와 새 절 추가, [[render-manual-deploy]] "배포를 피할 시간대"에 세 줄 보강(14시 교사알림·08시 보강명단이 빠져 있었다), [[textbook-fee]] 주간 흐름에 한 줄.
- 🔴 되짚어 둘 것: **`승인됨` 인 행만 나간다.** 08-21 실패의 진짜 원인은 배치가 아니라 승인이 안 된 것이었다. 시각을 다시 거는 것으로는 안 풀린다.

## [2026-08-22] ingest | 미도착 알림 + 등원 자동 체크

- 새 페이지 [[arrival-alert]] · 새 모듈 `api/arrivalAlertModule.js`(267줄) · 크론 14번째(매시 15·45분).
- **출석의 주인을 선생님에서 학생으로 옮겼다.** 아이들이 학원에 와서 플래너를 쓰기 때문에 `/save-progress` 저장이 곧 도착 신호다. 선생님 수동 체크와 `✅ 전체 출석`은 보정 수단으로 남긴다.
- 🔴 등원 체크는 **숙제 저장과 다른 PATCH**로 뺐다. `등원시각` 속성이 노션에 없으면 PATCH가 통째로 실패하는데 그것 때문에 아이 숙제 저장이 깨지면 안 된다.
- 🔴 **학부모 알림톡은 만들지 않았다.** 원장이 물었지만 도착 신호가 "플래너를 열었는가"라 오탐이 남아 있어 카카오워크만. 알림톡을 열려면 오탐률을 먼저 실측해야 한다.
- 가짜 노션으로 판정 7종 검증(출석됨/이미알림/결석사유/유예전/창밖/휴강/진도행없음) — 전부 의도대로.
- 갱신: [[cron-jobs]](13→14) · [[render-manual-deploy]] 배포 피할 시간대 · [[env-vars]] 3개 · [[routes]] · [[notion-databases]] · [[homework-automation]] · [[absence-notice]]

## [2026-08-22] ingest | 등원시간을 요일별 선택 칸으로 확정

- **아이마다·요일마다 시간이 다르다**(원장 확인). 텍스트 한 칸(`등원시간`)과 반 단위 시간표 DB를 검토하고 **요일별 선택 칸 6개**(`월등원`~`토등원`)로 정했다. 묶을 반이 없어 relation 방식은 탈락.
- 시간대: 월수금 14~17시, 화목 15~19시. **노션 선택 옵션은 속성마다 따로**라 요일 칸별로 그 요일 시간만 등록하면 드롭다운이 좁아진다.
- 🔴 **하루 한 번 등원시간 점검 알림을 추가했다**(`auditRoster`). 시각이 비거나 "2시"처럼 적히면 그 아이가 조용히 판정에서 빠지는데, 조용히 빠지는 것이 이 기능의 최악 실패다.
- 갱신: [[arrival-alert]] · [[notion-databases]] · [[env-vars]]

## [2026-08-23] ingest | 학생 프로필 카드 + 상담기록 DB

- 새 페이지 [[student-profile]] · 새 모듈 `api/studentProfileModule.js`(346줄) · 공용 위젯 `public/assets/student-profile.js`. 이름을 누르면 명부·문법·교재비·월간리포트·상담기록을 한 장으로 모은다. `teacher.html`(7탭+진도) · `past-grammar` · `passwords` · `results-viewer` 에 붙였다.
- **노션 `학생 상담기록` DB 신설**(`3c509320…`). 원래 요구는 "학생 명부에 열을 옆으로 계속 추가"였는데 명부 속성이 이미 88개라 별도 DB로 뺐다. 관계는 `single_property` — 명부에 역방향 열이 안 생긴다.
- 🔴 새 페이지 [[role-manager-is-not-owner]] — "원장만"을 `role === 'manager'` 로 걸었더니 **5명(조교 포함)이 통과했다.** 배포 전 발견, `loginId === 'manager'` 로 교체. 계정 7개 전부 실측해 원장 1명만 통과 확인.
- 🔴 새 페이지 [[local-server-fires-crons]] — 검증하려고 `node api/index.js` 를 띄울 뻔했다. 5분 크론 셋이 실전 알림톡을 쏜다. 모듈 하나만 올린 하네스로 우회.
- 🔴 [[arrival-alert]] 의 **요일별 옵션 결정을 내가 깰 뻔했다.** 등원시각 드롭다운을 요일 6개 합집합으로 뿌리고 있었다(월요일에 19:00 선택 가능 → 노션에 옵션 신설). 요일별로 갈랐고 저장 라우트도 그 요일 옵션 외에는 400으로 거절한다. **222칸을 채우는 자리가 노션 말고 하나 더 생겼다.**
- 실측: 프로필 응답 0.6~0.9초. 라이브 노션에 등원시각·상담기록 쓰기 시험 후 전부 원상복구.
- 갱신: [[notion-databases]](19개째) · [[env-vars]] · [[routes]] · [[views]](🔴 `/assets/*` 는 cache-first) · [[arrival-alert]] · [[counsel]] · [[auth-jwt]] · [[module-di]]
- **장부 정리**: `api/index.js` 가 16줄 늘어 위키 전체의 `file:line` 참조 84개가 밀렸다. HEAD 대비 라인 매핑으로 일괄 보정.

## [2026-08-25] ingest | 반 개명으로 막힌 문법 코멘트 저장 — select 필터 제거

- 🔴 새 페이지 [[grammar-class-rename]] — **M12B → M1B 개명.** 드롭다운은 `문법클래스` 롤업이라 즉시 따라왔는데 GRAMMAR_DB `반이름`(select) 옵션은 옛 이름 그대로였다. **노션은 등록되지 않은 옵션 이름으로 필터를 걸면 쿼리 자체를 거부한다** — 값을 못 찾는 게 아니라 400 이다.
- **한 원인이 네 증상으로 보였다.** 삼키는 정도가 경로마다 달랐다 — 코멘트만 저장은 눈에 보이게 실패(투사까지 중단), 전체 저장은 로그만 남기고 화면엔 성공, 프리필은 `console.error`, 프로필 카드는 `allSettled` 로 그냥 빈 칸. 하나가 터졌으면 같은 필터를 쓰는 나머지를 세어 보는 습관.
- 고침: `grammarRowFilter()` 로 **제목(`{반}-{날짜}`) 조회 통일**(3곳), 프로필 카드는 `title starts_with "{반}-"`(하이픈이 `M1B-` 와 `M12B-` 를 가른다), `ensureGrammarClassOption()` 이 쓰기 직전 옵션을 자동 등록(5분 캐시, 기존 옵션은 통째로 되돌려 보내 색 보존), `findPageIdByTitle` 의 죽은 `반이름` 분기 삭제.
- **`반이름` 은 남긴다.** 사람이 노션에서 눈으로 보는 칸이다. 다만 이제 찾는 데는 안 쓰고, 속성을 지워도 저장이 계속된다.
- 남은 것: 개명 전 `M12B-…` 행은 프로필 카드에서 안 잡힌다(최근 3건만 보는 자리라 감수). 고친 뒤 **M1B 를 한 번 다시 저장**해야 8/25 원장 행이 생긴다 — 전체 저장이 조용히 실패하는 동안 학생 행에만 들어갔다.
- 갱신: [[grammar-comment]](스키마 절에 열쇠 표) · [[notion-find-page]] · index.md pitfalls 표
- **장부 정리**: `api/index.js` 가 44줄 늘고 195행이 빠져 위키의 `file:line` 참조가 밀렸다. HEAD 대비 라인 매핑으로 26개 파일 일괄 보정.

## [2026-09-01] ingest | 월간 리포트 정리 — 소수점 누출·중복 로직·요약 잘림·마지막 주 누락

- 🔴 새 페이지 [[monthly-report-float-leak]] — 리포트 점수가 **`87.77777777777779점`** 으로 학부모에게 나갔다. 노션 수식이 실수를 돌려주는데 **어디서도 반올림하지 않았다.** 평균만 `Math.round` 해서 상단 타일은 멀쩡해 보였고, 새는 곳은 문법 막대·**Gemini 프롬프트**·학생 히스토리 팝업 셋. "수식에서 점수 꺼내기"가 파일 안에 **세 벌 복사**돼 있어 한 곳만 고쳐선 안 잡혔다. `getScoreFromFormula()` 한 곳으로 합쳤다.
- 🔴 [[monthly-report]] 를 `inferred` → `verified`. 744줄 정독. **통계·프롬프트·저장이 수동 라우트와 크론에 두 벌로 복사**돼 있었다(프롬프트만 30줄씩). 한쪽만 고치면 자동/수동 결과가 조용히 갈리는 구조. `generateMonthlyReport()` 하나로 합치고 734줄로 줄였다.
- 🔴 **크론이 매달 마지막 주를 통째로 빠뜨리고 있었다.** `Math.ceil(날짜/7) === 4` 라 22~28일에 도는데 조회 범위는 1일~말일. 2026-08은 22일에 돌아 **9일치**가 빠졌다. **마지막 토요일**(`day + 7 > 말일`)로 옮겼다 — 실행 횟수는 월 1회 그대로, 놓치는 날은 2026년 기준 옛 0~9일 → 새 0~6일. 남은 꼬리를 없애려면 '다음 달 1일 생성'인데 **원장 판단 대기**.
- 예전 크론은 리포트가 이미 있으면 `continue` 라 **통계 갱신조차 안 했다.** 이제 통계는 항상 PATCH, AI 재생성만 `force` 로 가른다(`aiSummary === null` 이면 `AI 요약` 속성 미접촉 → 기존 본문 보존).
- AI 요약을 `substring(0, 2000)` 으로 잘라 **문장 한가운데서 끊긴 채** 학부모 화면에 나갔다. `fitForNotion()` 이 마지막 문장 끝에서 자르고, 프롬프트에 1,200자 제한을 넣었다.
- 프롬프트가 소제목 4개를 요구하면서 **목록에 없는 `'💡 독해 및 문법'` 섹션**에 문법 분석을 넣으라고 지시하고 있었다. 강점/약점 섹션으로 옮겼다.
- 명부·진도 조회에 `has_more` 커서를 넣었다. 명부는 **95명이라 곧 100명을 넘고** 그러면 조용히 빠진다. 학생 한 명이 터져도 루프가 계속 돌게 per-student try/catch 추가(예전엔 전체가 멈췄다).
- 지역 재정의된 `getPropByKeywords` 사본 2개 제거, `api/index.js:950` 주입에 추가 → [[notion-prop-read]] · [[module-di]]
- 검증: 라이브 노션을 건드리지 않고 가짜 `fetchNotion`/`geminiModel` 하네스로 생성·갱신·렌더 3경로 실행 → [[local-server-fires-crons]]. `87.77777777777779` → `88`, 페이지·프롬프트·저장 props 전체에서 소수점 0건, 요약은 문장 끝에서 절단 확인.
- 갱신: [[cron-jobs]](토요일 행) · [[monthly-report]] · index

## [2026-09-01] ingest | 월간 리포트 생성일을 매월 1일로 (원장 확정)

- 🔴 **한 달이 닫힌 뒤에 집계한다.** 같은 날 앞서 4번째 토요일 → 마지막 토요일로 옮겼지만 **둘 다 그 달 안에서 도는 방식**이라 꼬리가 남았다(마지막 토요일 기준 0~6일). `0 9 1 * *` + `prevMonth()` 로 **지난달**을 만든다 → 누락 0일.
- **09:00 을 고른 이유**: 1일이 월요일이면 10:00 에 조교 장보기 크론(`0 10 * * 1`)이 있다(2026년엔 6/1). 95명 × 2초 ≈ 4분이라 10:20 데일리 리포트 생성 전에 끝난다.
- 🔴 **새 위험: 이 크론은 이제 월 1회뿐이다.** 배포와 겹쳐 건너뛰면 다음 기회가 **한 달 뒤**다. 5분·매일 크론과 달리 저절로 복구되지 않는다. 그래서 `/add-cron` 체크리스트대로 두 가지를 같이 넣었다:
  - 수동 트리거 `POST /api/monthly-report/tick` (`?month=` 없으면 지난달). 배치를 `runMonthlyReportBatch()` 로 빼서 크론과 공유. 멱등.
  - 실패 통지를 **원장 DM**(`KAKAOWORK_APPROVAL_CONV`)으로. 실패 명단 + 복구 명령을 본문에 담는다. 예전엔 콘솔에만 남았다 → [[kakaowork-notify]]
- 검증: 실제 소스에서 `prevMonth` 를 뽑아 연말 넘김 확인(`2026-01-01` → `2025-12`). 하네스로 배치 실행 — 명부 2쪽 페이지네이션, 데이터 없는 학생 skip, 한 명이 `409` 로 터져도 나머지 진행, 조회 범위가 `2026-08-01~2026-08-31` 로 닫힘, 잘못된 `month` 는 400. 라이브 노션 미접촉 → [[local-server-fires-crons]]
- ⚠️ **알아둘 회귀**: 1일 00:00~09:00 사이엔 지난달 리포트가 아직 없다. 그때 `📅 월간` 을 누르면 404 다. 이전엔 마지막 토요일에 이미 만들어져 있었다.
- 갱신: [[monthly-report]] · [[cron-jobs]](토요일 행 → 1일 행) · [[render-manual-deploy]](배포 피할 시간대) · [[routes]] · index

## [2026-09-01] ingest | 세션 시작 훅 — 원격과 어긋났는지 먼저 본다

- 🔴 새 페이지 [[stale-head-line-refs]] — 같은 날 월간 리포트 작업을 **8일 뒤처진 HEAD** 에서 했다. 코드는 자동 병합돼 멀쩡했지만 **위키에 적은 `api/index.js:951` 이 실제로는 950** 이었다. git 은 push 할 때야 막아주는데 그땐 이미 틀린 번호를 적은 뒤고, 라인 번호는 틀려도 lint 에 안 걸린다.
- 원격 커밋(`f7c0405`)이 `api/index.js` 를 44줄 늘리고 195행을 뺀 것이라, **그 커밋 자신도 26개 파일의 `file:line` 을 보정**하고 있었다. 같은 함정을 같은 파일에서 두 번 밟았다.
- 고침: `.claude/settings.json` 신설 + `.claude/hooks/check-remote.sh`. SessionStart 에 `git fetch` 하고 뒤처졌을 때만 컨텍스트에 알린다(timeout 20). 동기·앞섬이면 무출력, 네트워크·인증 실패해도 `exit 0`.
- 🔴 **훅이 `pull` 을 대신 하지 않는다.** 이 저장소는 서브모듈(`academy-planner/…`)이 상시 더러워서 무턱댄 pull 이 상태를 섞는다. 알려만 주고 판단은 사람이 한다.
- CLAUDE.md 에 한 줄 추가(48→50줄, 목표 50 이내 유지). 근거는 위키로.
- 검증: 동기 상태에서 무출력 확인, `HEAD~1` 추적 브랜치로 뒤처짐 재현해 JSON 출력 확인 후 브랜치 정리.
- 갱신: index pitfalls 표 · `CLAUDE.md`

## [2026-09-01] ingest | 노션 스키마 드리프트 점검 (다듬기 1/3)

- 새 페이지 [[schema-check]] · 새 모듈 `api/schemaCheckModule.js`(144줄) + 선언 `api/notionSchema.js`(83줄). **이 저장소 1번 사고 유형의 안전망이다** — pitfalls 7장 중 3장이 "노션에서 이름을 바꿨더니 코드가 조용히 멈춘 것"이었다.
- 실측: 노션 속성 직접 접근 **179곳, 그중 이름이 바뀌어도 잡는 폴백은 13곳(7%)**. DB 속성 목록을 조회하는 코드는 **아예 없었다**.
- 선언 목록은 코드에서 뽑았다 — 쿼리 **필터·정렬**(12개 DB·30속성)과 **쓰기**(POST/PATCH properties)를 추출해 14개 DB 로 정리. 🔴 읽기 전용 속성은 일부러 뺐다: 없으면 `undefined` 로 흐를 뿐 400 이 아니라서, 넣으면 알림만 시끄러워진다.
- 설계 원칙 셋: **정상이면 무소음**(매일 '이상 없음'이 오면 곧 무시된다) · **속성이 늘어난 건 안 본다**(칸 추가마다 알림이 오면 아무도 안 본다) · **공백 정규화로 개명 후보를 따로 보고**(`teacher-rollup-name` 때 원인 찾는 데 가장 오래 걸린 지점).
- `404` 도 보고한다 — 통합 권한이 DB 에서 빠지는 것도 조용한 킬러다.
- 검증: 하네스로 5경우 — 전부 정상(무소음) · 속성 소멸 · **NBSP 개명 탐지** · DB 못 읽음 · tick 응답. 라이브 노션 미접촉 → [[local-server-fires-crons]]
- 갱신: [[cron-jobs]](14→15) · [[render-manual-deploy]](07:25~07:35) · [[routes]] · [[teacher-rollup-name]] · [[grammar-class-rename]] · [[textbook-name-whitespace]] · index

## [2026-09-01] ingest | 테스트 스캐폴드 — `npm test` (다듬기 2/3)

- 새 페이지 [[test-harness]] · `test/fakes.mjs` + 테스트 2벌(월간 리포트 10 · 스키마 점검 7) = **17개 전부 통과**. 러너는 Node 내장 `node --test` 라 의존성이 0 이다.
- 왜: 이 저장소는 **로컬에서 서버를 못 띄운다**(5분 크론이 실전 발송). 그래서 매 세션 같은 하네스를 새로 짜고 버렸다 — 오늘 하루에만 네 번.
- 🔴 **스캐폴드를 만들자마자 진짜 결함이 하나 나왔다.** `monthlyReportModule` 이 `node-cron` 을 직접 import 하고 있어서, 가짜 크론을 넘겨도 진짜 스케줄러가 뜨고 **테스트가 안 끝났다**(180초 타임아웃). `dependencies.cron || nodeCron` 으로 고쳤다 → [[module-di]]
- 하네스가 기록형이라 "무엇을 어떻게 물었나"까지 검사한다 — 페이지네이션 커서 추적, 조회 범위가 말일까지 닫혔는지, AI 를 다시 불렀는지(비용).
- 실패 메시지를 **증상으로** 적었다: '학부모 화면에 87.7777… 이 남았다', '문장 중간에서 끊겼다', 'AI 를 다시 불렀다 — 비용'. 나중에 깨졌을 때 뭐가 문제인지 바로 보이게.
- 처음 두 번은 **테스트가 틀렸다**(88·67 평균을 77로 계산, 페이지네이션 기댓값 오독). 코드가 아니라 기댓값을 고쳤다.
- 갱신: [[local-server-fires-crons]](대안 포인터) · [[monthly-report]]('남은 것' 하나 해소) · `package.json` · index

## [2026-09-01] ingest | 데일리 리포트를 모듈로 분리 (다듬기 3/3)

- `api/dailyReportModule.js`(365줄) 신설. **`api/index.js` 3,842 → 3,506줄.** 리포트 HTML·`/report`·학생 본인 조회·URL 채우기·크론 둘(10:20 생성, 22:00 URL)을 통째로 옮겼다.
- **코드는 한 줄도 바꾸지 않았다.** 구역이 자립적인지 먼저 재고(구역 안 정의 6개가 밖에서 안 쓰이는 것 확인), 바깥에서 쓰던 것 15개만 주입으로 받는다. `getActivePause`·`readStudentConfigs` 는 호출보다 뒤에 있지만 `async function` 선언이라 호이스팅된다 — 확인함.
- 🔴 [[module-di]] 에 규칙 하나 추가: **`cron` 도 주입받아라.** 안 그러면 테스트가 안 끝난다.
- 검증: [[test-harness]] 로 8개 테스트 추가(라우트 7개 존재 · 크론 둘 · 생성 · 중복 건너뜀 · 요일 불일치 · 휴강 · URL 채우기 · URL 동일하면 안 씀). **전체 25개 통과.**

### 장부 정리 — 여기서 오늘 두 번째로 밟을 뻔했다

- `index.js` 가 335줄 줄어 위키의 `file:line` 참조가 전부 밀렸다. HEAD 대비 difflib 매핑으로 **89개 보정** 후 **옛 줄 내용 vs 새 줄 내용을 전수 대조**(87 일치).
- 🔴 **내용 대조만으로는 부족하다.** `});` 같은 무의미한 줄은 우연히 일치해서 통과한다. 그래서 (a) 옮겨간 구역(HEAD 2498~2843)을 가리키던 참조를 좌표로 따로 색출하고, (b) 오늘 손댄 참조 21개는 **토큰으로 실측**했다.
- 🔴 **`` `:2657` `` 같은 짧은 표기는 정규식이 안 잡는다.** 27개가 그렇게 숨어 있었고 [[daily-report]] 의 5개는 옛 번호 그대로 틀려 있었다. 페이지의 `source:` 를 문맥으로 삼아 따로 보정했다 — 다만 `source:` 에 파일이 둘이면 그 규칙이 또 틀린다(실제로 [[daily-report]] 를 한 번 잘못 밀었다가 되돌렸다). **짧은 표기 대신 전체 경로를 써라.**
- 이번 세션에 **현재 파일 기준으로** 적은 참조까지 옛 좌표로 취급해 밀어버린 사고가 하나 있었다([[schema-check]] 1050→1071). 실측으로 되돌렸다. 매핑은 HEAD 시점 참조에만 적용해야 한다.
- 갱신: [[cron-jobs]] · [[routes]] · [[daily-report]] · [[monthly-report]] · [[module-di]] · [[schema-check]] · env-vars · auth-jwt · textbook-name-whitespace 외 다수

## [2026-09-03] ingest | KAKAOWORK_APPROVAL_CONV 는 Render 에 설정돼 있다 (원장 확인)

- [[schema-check]] 알림이 실제로 어디로 가는지 물어봐서 추적했다. **원장 카카오워크 1:1 DM** — 교재비 승인 요청이 오는 그 방이다.
- 🔴 확인 중 알게 된 것: `KAKAOWORK_APPROVAL_CONV` 는 **폴백 상수가 없는 유일한 방**이다. 상담(`…560321`)·결석(`…560320`)은 코드에 ID 가 박혀 있어 env 가 비어도 가지만, 승인 방은 `|| ''` 라 비면 `notifyOwner` 가 `null` 이 되고 원장 알림이 전부 Render 로그로만 간다. **조용한 실패를 막으려는 기능이 조용히 실패하는 모양**이라 표에 적어 뒀다.
- 원장 확인: **설정돼 있다.** 그리고 원장이 ID(`1004769595384591`)를 줘서 **코드에 폴백으로 박았다**(`api/index.js:339`) — 상담·결석 방과 같은 모양이 됐다. 이제 env 가 지워져도 원장 알림이 안 끊긴다.
- 곁가지: 미도착 알림의 `ARRIVAL || APPROVAL || ABSENCE` 3단 폴백에서 마지막 칸이 죽었다(APPROVAL 이 이제 안 비므로). 의도가 "첫 주는 원장 DM 으로만"이었으니 동작은 그대로다 → [[arrival-alert]]
- 갱신: [[env-vars]] · [[schema-check]] · `api/index.js`