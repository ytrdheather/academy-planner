# 진도 자동화 (개별 어휘/독해 숙제 자동 생성) — 핸드오프 문서

> 이 문서는 다른 기기(직장 PC 등)에서 작업을 이어가기 위한 자립형 핸드오프입니다.
> 새 Claude 대화에서 이 파일을 읽으면 지금까지의 설계·구현·남은 일을 그대로 이어받을 수 있습니다.
> 최종 업데이트: 2026-07-15 / 관련 커밋: `9a533ce`

---

## 0. 한 줄 요약

리디튜드 학원의 **개별 진도(어휘/주독해/부독해) 숙제를 매일 수기로 쓰는 부담**을 없애기 위해,
노션 기반으로 **학생별 진도 설정 → 등원일마다 다음 숙제를 자동 생성(미리보기→확정)** 하는 시스템.
현재 **진도 설정 UI + 자동 생성 엔진(Phase 4)** 까지 구현·배포 완료. 남은 건 학습상태 연동·커서 힌트·휴일 연동.

---

## 1. 핵심 개념 (숙제 세팅 로직)

### 1-1. 데이터 모델: "과목별 · 등원요일별 개수 맵"
- 등원일에 내주는 숙제 = **"다음 등원까지 완료해야 할 최소단위(지문) 개수"**.
- 이 개수는 **마감일 기준**으로 표현: `월:2` = "월요일 수업 올 때까지 2개 완료".
- 숙제 출력 예: **"월요일(7/20)까지 2개 해오기"** (요일 + 날짜 병기).

### 1-2. 단위(1개 = 지문 1행)
- "1개" = 교재 세부 내용 DB의 **`제목` 1행**(가장 작은 학습단위 = 1지문/1lesson).
- 워크북은 별도 커서 없이 **주교재 유닛을 그대로 따라감**(없으면 생략).

### 1-3. 입력 3모드 (엔진이 요일별 맵을 자동 생성)
| 모드 | 의미 | 계산 |
|---|---|---|
| **등원마다 N** | 나올 때마다 N개 | 각 등원요일 = N |
| **매일 N** | 하루 N개 페이스 | 직전 등원 다음날 ~ 이번 등원일, **일요일만 제외**, 하루 N개 (주간합 6N) |
| **불규칙** | 요일별 직접 지정 | 텍스트 "월2 수1 금1" |

**매일 모드 예시 (하루 1개):**
- 월수금 → 월2·수2·금2 (합6)
- 화목 → 화4·목2 (합6)
- 월금 → 월2·금4 (합6)
- 월수 → 월4·수2 (합6)

### 1-4. 커서 = 지문 순번 (1-based)
- `{과목}현재유닛` 칸 = **다음에 풀 지문의 순번**(교재 세부 내용 DB의 `순번`).
- 유닛당 1지문 교재(대부분) → 순번 = 유닛번호. "Unit 6부터" → 커서 `6`.
- 유닛당 2지문 교재 → Unit 6 첫 지문 = 순번 `(6-1)×2+1 = 11`.
- 비워두면 엔진이 `1`(처음)부터 시작.

### 1-5. 확정된 정책 결정 (2026-07-15)
- **기록 위치**: 오늘(배정일) 행. (학생 플래너가 today 행을 읽으므로 지금 바로 보임)
- **커서 의미**: 지문 순번(전체 순서).
- **중복 방지**: 확정은 **빈 숙제칸에만** 기록(이미 있으면 스킵) → 두 번 눌러도 커서 이중 전진 없음.
- **주/부/어휘 완전 독립**, 부교재 없으면(relation 빈칸) 그 과목 스킵.

---

## 2. 노션 DB 구조

| 역할 | DB | env 변수 | ID |
|---|---|---|---|
| 학생 명부(설정 원천) | New 학생 명부 관리 | `STUDENT_DATABASE_ID` | `25409320-bce2-80f8-ace1-ddcdd022b360` |
| 일일 진도(숙제 출력) | NEW 리디튜드 학생 진도 관리 | `PROGRESS_DATABASE_ID` | `25409320-bce2-8076-97ed-e3f1c1b62ada` |
| 교재 마스터 | 교재 데이터베이스 | `TEXTBOOK_DB_ID` | `18f09320bce2800aac09c7856bf17e7d` |
| 교재 세부 내용(목차) | 교재 세부 내용 DB | `TEXTBOOK_UNIT_DB_ID` | `39b09320bce28173a7fce26ecbec70b6` |
| 숙제 정지 기간(킬스위치) | 숙제 정지 기간 | `PAUSE_DB_ID`(코드 폴백 있음) | `39e09320-bce2-8115-aa02-f03fabca5433` |

> Notion 인증: env `NOTION_ACCESS_TOKEN`. 모든 env는 로컬 `.env` + Render에 이미 설정됨 (이번 배포에 새 env 불필요).

### 2-1. 학생 명부 DB — 과목별 설정 칸 (어휘/주독해/부독해 각각)
| 칸 | 타입 | 비고 |
|---|---|---|
| `{과목}교재` | relation → 교재 DB | 비우면 그 과목 스킵 |
| `{과목}현재유닛` | number | 커서 = 지문 순번 |
| `{과목}진도량` | number | N |
| `{과목}진도방식` | select: 등원마다·매일·불규칙 | 🆕 2026-07-15 추가. 빈값 = 등원마다 취급 |
| `{과목}요일별진도량` | rich_text | 🆕 불규칙일 때만. "월2 수1 금1" |
| `{과목}직전배정량` | number | 🆕 2026-07-15. 확정 시 기록, 미룸(↩) 롤백량 |

- 공용: `수강요일`(multi_select, 옵션 **토·월·화·수·목·금** — 일요일 없음), `학습상태`(select: 정상/병결/일시휴원/여행휴가/내신대비/휴원), `고정숙제`(rich_text).

### 2-2. 교재 데이터베이스 주요 칸
- `교재이름`(title), `과목`(select: 독해서/어휘서/문법서/…), `총유닛수`(number), `유닛당지문수`(number, 기본1), `워크북`(checkbox).

### 2-3. 교재 세부 내용 DB (목차) — "최소단위=1행" 평면 모델
- `순번`(number, 진도커서=책 전체 순서), `그룹`(rich_text, "Unit 1 Food"·"Chapter 1"), `분류`(rich_text, Subject), `제목`(title), `시작페이지`/`끝페이지`(number), `교재`(relation).
- 입력 도구: `/textbook-toc` 페이지 (대시보드 원장 전용 `📚 교재 목차 입력` 버튼). Gemini 비전으로 목차 이미지→구조화.

### 2-4. 일일 진도 DB — 관련 칸
- `이름`(title, 조인 키), `🕐 날짜`(date), `출석`(checkbox), `결석 사유`(rich_text, 있으면 결석), `4.수업요일`(rollup→수강요일).
- 숙제 출력칸: `어휘숙제`·`주독해숙제`·`부독해숙제`(rich_text), `문법 숙제 내용`(rich_text).
- 행은 매일 아침 10시 노션 자동화로 생성됨(그날 등원자).

---

## 3. 구현 완료 (커밋 9a533ce, 배포됨)

### 3-1. 진도 관리 탭 UI (`public/views/teacher.html`)
- 과목별 '진도량·방식' 셀 = `amountCell(s, key)`:
  - 교재 없으면 "교재 없음" 스킵 표시.
  - 있으면 `[N number][방식 select]` + 아래 프리뷰.
  - 등원마다/매일 → 엔진 계산 요일별 회색 프리뷰(`computePreview`).
  - 불규칙 → 그 학생 수강요일만 개별 입력칸(`saveWeeklyDay`가 문자열 재구성).
- 헬퍼: `parseAttendDays`/`studyDaysBetween`(일요일만 제외)/`computeWeeklyMap`/`computePreview`/`parseWeekly`/`stringifyWeekly`.
- `saveMethod` = 저장 후 `renderProgressConfig`(불규칙칸 토글).

### 3-2. 백엔드 (`api/index.js`)
- `loadTextbooks`: 항목에 `perPassage`(유닛당지문수) 추가.
- `readStudentConfigs()`: 학생 명부 읽기 재사용 함수. `GET /api/progress-config-data`가 이것 호출(과목별 `method`·`weekly` 포함 반환).
- `POST /api/update-student-progress`: 범용 저장(relation/number/select/rich_text). 새 칸 저장도 이걸로.

### 3-3. Phase 4 자동생성 엔진 (`api/index.js` 하단 블록)
- 엔진 헬퍼(백엔드 자체 사본): `parseAttendDays`·`studyDaysBetween`·`computeWeeklyMap`·`parseWeeklyStr`·`deadlineQuantity`·`nextClassInfo`(오늘→다음 등원일, UTC 정오 기준 요일 계산)·`unitLabel`·`buildAssignment`.
- `buildAssignment(book, units, cursor, count, deadlineLabel)`:
  - 목차 있으면: 순번≥커서 count개 → `"그룹 제목 ~ 그룹 제목 (p.시작~끝) + 워크북 그룹범위"`, newCursor = 커서+count.
  - 목차 없으면: 유닛당지문수로 `"Unit A~B"` 환산.
  - 교재 끝 도달 시 `reachedEnd` 플래그.
- `POST /api/generate-homework-preview`: 오늘 일일행 fetch → 이름으로 명부 조인 → **출석 && 결석사유 없음 && 학습상태(빈값=정상)** 필터 → 과목별 마감 개수만큼 지문 뽑아 제안. **아무것도 안 씀.** 이미 있는 칸 `alreadyFilled` 표시.
- `POST /api/confirm-homework`: 선택 학생들의 일일행 숙제칸 rich_text 기록 + 명부 커서 number 전진. 스트리밍 진행률.

### 3-4. 출결·숙제 탭 UI
- `🔮 다음 숙제 생성` 버튼 + 미리보기 모달(`hwPreviewOverlay`):
  - 학생별 과목 체크박스(이미있음=해제) + 문구 수정칸.
  - `openHomeworkPreview`/`renderHomeworkPreview`/`confirmHomework`/`closeHomeworkPreview`.
  - 확정 후 `loadDataForCurrentTab(true)` 새로고침.

### 3-6. 전원생 숙제 정지(킬스위치) — 2026-07-15 추가
- **동작**: **생성일 기준** — 오늘이 '활성' 정지기간(시작일~종료일) 안이면 `generate-homework-preview`가 `{paused:true, pause, students:[]}` 반환 → 아무에게도 부과 안 됨.
- **저장**: 노션 `숙제 정지 기간` DB(사유 title / 시작일·종료일 date / 활성 checkbox). 여러 기간 예약 가능(공휴일·방학·임시휴원). `PAUSE_DB_ID`는 env 없으면 코드 상수로 폴백(Render env 추가 불필요).
- **백엔드**: `getActivePause(dateStr)` + `GET /api/pause-periods`(목록+activeNow) / `POST /api/pause-periods`(추가) / `POST /api/pause-periods/update`(활성토글·archive삭제). 조회 실패 시 생성을 막지 않음(안전).
- **프론트**: 출결·숙제 탭 `⏸️ 정지 관리` 버튼 + 관리 모달(사유·기간 추가/삭제/활성토글), 현재 정지 중이면 버튼 옆 빨간 배너(`refreshPauseBanner`, 탭 진입 시 갱신), 미리보기 모달도 정지 화면 표시.
- 이 DB는 향후 **휴일 연동(마감일 이동)** 의 데이터 소스로 재활용 가능.

### 3-7. 숙제 미룸/이월(↩) — 2026-07-15 추가
- **문제**: 확정 시 커서가 전진하므로, 학생이 특정 과목 숙제를 안 하면 그 범위가 지나가버림.
- **해법(A방식: 밀린 것만)**: 그 과목 커서를 **직전 배정량만큼 되돌림** → 다음 생성 때 같은 범위가 새 마감으로 재출제. 따라잡을 때까지 새 진도는 멈춤.
- **명부 신규 칸**: `{과목}직전배정량`(number) ×3. `confirm-homework` 확정 시 각 과목 `advanced`(= newCursor − 직전커서) 기록.
- **백엔드**: preview 과목 출력에 `advanced`·`lastAmtField` 추가. `POST /api/defer-homework {name, prefix}`(prefix∈어휘/주독해/부독해): 명부 이름조회 → 커서 −= 직전배정량(최소1), 직전배정량 0으로(중복 미룸 방지).
- **프론트**: 출결·숙제 탭 어휘/주독해/부독해 숙제칸마다 `↩` 버튼 → `deferHomework(name, prefix)`. (문법칸은 없음)
- **라이브 검증**: 홍상연 설정→확정(현재유닛3·직전배정량2)→미룸(커서3→1·직전배정량0)→중복미룸 거부→원복.

### 3-5. 검증 내역 (라이브)
- 순수 함수 단위테스트: 목차/워크북/교재끝/2지문 환산/마감요일/개수 전부 통과.
- 실제 노션 통합(자기복구): 홍상연(화목, 2026-07-14) 임시 설정 → 프리뷰 `"목요일(7/16)까지 2개: CHAPTER 1 River The Nile River ~ …Waltz (p.6~19)"` (실 유닛 제목) 정상 → 확정 시 커서 1→3 전진 + 일일행 기록 → **전부 원복(잔여 0)**.

---

## 4. 남은 작업 (설계 확정, 미구현)

### 4-1. ③ 학습상태 연동 — ✅ 완료 (2026-07-15)
`학습상태 ≠ 정상`일 때 3가지 자동 동작 모두 구현:
1. **숙제 생성 정지** — ✅ 엔진이 스킵(빈값=정상 취급).
2. **누락 점검 제외** — ✅ `computeMissing`이 `isAbsent(st) || statusExcluded(st)`로 제외.
3. **상태 배지** — ✅ `buildStatusBadges`가 결석 다음 우선순위로 상태 배지 표시. 매핑 `STATUS_BADGE`: 🤒병결/⏸️일시휴원/🏖️여행휴가/📚내신대비/💤휴원(그 외 상태는 ⏸️+이름). 출결·숙제 탭 이름 옆(att-absent-badge 슬롯)에도 결석 아니면 상태 배지 표시.
- **구현 핵심**: 학습상태는 명부에만 있어 일일 데이터에 없었음 → 일일 DB에 `학습상태` **롤업 추가**(relation `학생 명부 관리`, show_original) → parseDailyReportData가 `learningStatus` 반환 → 프론트 `statusExcluded`/`statusBadgeInfo` 헬퍼.
- 라이브 검증: 홍상연 여행휴가 설정 시 일일데이터 learningStatus='여행휴가' 반영, 누락점검서 휴가학생 제외·정상학생만 잡힘, 배지 문구 정상 → 원복.

### 4-2. 커서 힌트 (중간 시작 안전장치) — ✅ 완료 (2026-07-15)
- 진도 관리 탭 유닛(커서) 칸 아래에 그 순번의 지문 제목을 실시간 표시(예: `→ CHAPTER 1 River The Heron... p.20`).
- 구현: teacher.html `cursorCell(s,key)`(유닛 input + 힌트 span), `loadUnitsForBook(bookId)`(캐시, `/api/textbook-units` 재사용), `updateCursorHint(input)`(oninput), `populateCursorHints()`(renderProgressConfig 후 호출). 목차 없으면 `Unit N`, 범위 밖이면 `교재 끝(범위 밖)`.
- 라이브 검증: 홍상연 목차책 커서3→"The Heron p.20", 1→"The Nile River p.6", 99→"교재 끝" 확인 후 원복.

### 4-3. 휴일 연동 — ✅ 완료 (2026-07-15)
- **결정 반영**: 휴무일 소스 = **기존 `숙제 정지 기간` DB 재사용**(새 DB 안 만듦). **휴일도 숙제일로 셈**(개수는 일요일만 제외). 공휴일 자동 프리필 = 오프라인 `date-holidays`(KR, 음력·대체공휴일 내장, `npm i date-holidays`).
- **엔진**: `nextClassInfo(todayStr, attendArr, isHoliday)` — 최대 28일 앞까지 보며 수강요일이면서 휴무일 아닌 첫 개원일 반환(+`dateStr`,`studyDays`=오늘~그날 일요일제외 날수). `deadlineQuantity(subjCfg, nc)` — 매일=`N*nc.studyDays`(실제 날짜 기반), 등원마다=N, 불규칙=weekly[nc.char]. 날짜유틸 `_dUTC`/`_fmtUTC`/`nonSundayCount`. (백엔드 `computeWeeklyMap`/`studyDaysBetween`는 이제 미사용.)
- **정지/휴무 판정**: preview가 `getActivePausePeriodList()`(활성 전체)를 한 번 불러 `isHoliday(ds)`=어떤 활성기간에 포함되나, `pauseNow`=오늘이 포함되면 전체 생성 중단. 즉 정지기간=휴무일 이중 효과(그날 생성막음 + 마감 이동).
- **프리필 엔드포인트** `POST /api/prefill-holidays {year}`: date-holidays KR public 공휴일을 정지기간 행으로 생성(사유=`[공휴일] 이름`, 시작=종료=그날, 활성). 기존 시작일과 중복 제외. 프론트: 정지 관리 모달에 `🎌 공휴일 채우기`(+연도 입력)·`prefillHolidays()`.
- 진도 관리 탭 요일별 프리뷰는 "평상시 한 주" 예시로 유지(프론트 계산은 패턴 기반 그대로, 실제 생성만 날짜/휴일 반영).
- 라이브 검증: 엔진 단위테스트(금 휴일→마감 월요일 이동·매일 개수 4로 증가·등원마다 유지), 프리필 2027 12개 등록·중복0·원복.

---

## 5. 알려진 이슈 / 주의사항

- ⚠️ **교재 relation 정리 필요**: 다수 학생의 `어휘교재` relation에 여러 책이 섞여 있음(레거시). 엔진은 `relation[0]`만 사용. **실사용 전 과목별 단일 교재로 정리** 필요.
- 오늘(생성 시점) 일일행이 있어야 미리보기 대상이 잡힘(행은 아침 10시 자동 생성).
- Windows 셸에서 curl `-d`에 한글/이모지 속성명을 직접 넣으면 인코딩 깨짐 → payload는 파일로 만들어 `--data-binary @file` 사용.

---

## 6. 이어서 작업하는 법

### 6-1. 환경
- 저장소: `github.com/ytrdheather/academy-planner`, 기본 브랜치 `main` (Render 자동 배포).
- 시작 스크립트: `node api/index.js` (포트 `PORT` env, 기본 5001).
- 배포용 실 코드는 **루트** `api/index.js` + `public/views/teacher.html`. (`academy-planner/uri-hagweon-gwanri-peurogeuraem/` 하위는 별개 — 건드리지 말 것.)

### 6-2. 로컬 테스트 팁
- 서버: `node api/index.js` → `http://localhost:5001/teacher`.
- 교사 페이지는 `localStorage['teacher_auth_token']` 필요(JWT). 로컬 검증 시 `.env`의 `JWT_SECRET`으로 매니저 토큰 발급해 주입.
- 라이브 노션 데이터로 검증할 땐 **반드시 자기복구(원복)** 방식으로(위 3-5 참고).

### 6-3. 다음 세션 시작 문구 예시
> "docs/진도자동화-핸드오프.md 읽고, ③ 학습상태 연동부터 이어서 하자."

---

## 부록: 빌드 순서 전체
1. 노션 구조 ✅
2. 진도 관리 탭 UI ✅ (커밋 9a533ce)
3. Phase 4 자동생성 엔진 ✅ (커밋 9a533ce)
3.5. 전원생 숙제 정지(킬스위치) ✅ (2026-07-15)
3.7. 숙제 미룸/이월(↩) ✅ (2026-07-15)
4. ③ 학습상태 연동 ✅ (2026-07-15)
5. 커서 힌트 ✅ (2026-07-15)
6. 휴일 연동 ✅ (2026-07-15)
7. (안정화 후) 반 단위 버튼 → cron 자동화 ⬜  ← 로드맵상 마지막 남은 항목
